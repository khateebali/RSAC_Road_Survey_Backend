package com.gnn.roadsurvey.entity;

public enum OrgType {
    RSAC,          // state-level oversight — selects a city per session, not fixed on profile
    NAGAR_NIGAM    // e.g. Ghaziabad — permanently fixed to one city on the profile
}
